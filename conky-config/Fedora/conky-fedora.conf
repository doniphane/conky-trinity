conky.config = {
    
    alignment = 'top_right',
    gap_x = 50,
    gap_y = 50,
    minimum_width = 300,
    maximum_width = 500,
    
    -- Apparence
    background = true,
    border_width = 1,
    draw_borders = true,
    draw_outline = false,
    draw_shades = true,
    draw_graph_borders = true,
    
    -- Couleurs Fedora (bleu foncé/clair + accents)
    default_color = 'e8e8e8',
    default_outline_color = 'white',
    default_shade_color = '0b1015',
    color1 = '3c6eb4',  -- Bleu Fedora principal
    color2 = '51a2da',  -- Bleu clair Fedora
    color3 = 'f17a65',  -- Orange accent
    color4 = 'a0d995',  -- Vert accent
    color5 = '294172',  -- Bleu foncé
    
    -- Fenêtre avec style Fedora
    own_window = true,
    own_window_type = 'desktop',
    xinerama_head = 1,
    own_window_transparent = false,
    own_window_argb_visual = true,
    own_window_argb_value = 200,
    own_window_colour = '0f1419',
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    
    -- Police
    use_xft = true,
    font = 'Roboto:size=9',
    xftalpha = 0.9,
    
    -- Performances
    update_interval = 2.0,
    cpu_avg_samples = 2,
    net_avg_samples = 2,
    no_buffers = true,
    double_buffer = true,
    
    -- Autres
    uppercase = false,
    use_spacer = 'none',
    show_graph_scale = false,
    show_graph_range = false,
    
    -- Script Lua pour détection automatique
    lua_load = '/home/noelson/.config/conky/conky-auto.lua',
}

conky.text = [[
${alignc}${color1}╔═══════════════════════════════╗
${alignc}${color1}║${color2}      FEDORA MONITORING      ${color1}║
${alignc}${color1}╚═══════════════════════════════╝${color}

${alignc}${color2}${font Roboto:bold:size=11}${time %H:%M}${font}
${alignc}${color}${font Roboto:size=11}${time %A %d %B %Y}${font}

${color1}┌─[ SYSTÈME ]${hr 2}${color}
${color2}│ OS:${color} ${execi 86400 cat /etc/fedora-release}
${color2}│ Kernel:${color} ${kernel}
${color2}│ Uptime:${color} ${uptime_short}
${color2}│ Hostname:${color} ${nodename}
${color1}└──────────────────────────────${color}

${color1}┌─[ PROCESSEUR ]${hr 2}${color}
${color2}│ Modèle:${color} ${execi 86400 cat /proc/cpuinfo | grep 'model name' | head -1 | cut -d':' -f2 | cut -c 2-30}
${color2}│ Fréquence:${color} ${freq_g} GHz
${color2}│ Température:${color} ${acpitemp}°C
${color2}│ Charge:${color} ${cpu}% ${alignr}${color5}${cpubar 8,120}
${color2}├─${color} CPU 1: ${cpu cpu1}% ${alignr}${color5}${cpubar 6,100 cpu1}
${color2}├─${color} CPU 2: ${cpu cpu2}% ${alignr}${color5}${cpubar 6,100 cpu2}
${color2}├─${color} CPU 3: ${cpu cpu3}% ${alignr}${color5}${cpubar 6,100 cpu3}
${color2}└─${color} CPU 4: ${cpu cpu4}% ${alignr}${color5}${cpubar 6,100 cpu4}
${color1}│${color} ${cpugraph 35,298 294172 51a2da -t}
${color1}└──────────────────────────────${color}

${color1}┌─[ MÉMOIRE ]${hr 2}${color}
${color2}│ RAM:${color} ${mem} / ${memmax} ${alignr}${memperc}%
${color1}│${color} ${membar 8}
${color2}│ Swap:${color} ${swap} / ${swapmax} ${alignr}${swapperc}%
${color1}│${color} ${swapbar 8}
${color1}└──────────────────────────────${color}

${color1}┌─[ STOCKAGE ]${hr 2}${color}
${color2}│ Racine (/):${color}
${color2}│${color}   ${fs_used /} / ${fs_size /} ${alignr}${fs_used_perc /}%
${color1}│${color}   ${fs_bar 8 /}
${color1}└──────────────────────────────${color}

${color1}┌─[ RÉSEAU ]${hr 2}${color}
${lua_parse conky_show_network_fedora}


${color1}┌─[ GPU NVIDIA ]${hr 2}${color}
${color2}│ Modèle:${color} ${execi 86400 /usr/bin/nvidia-smi --query-gpu=name --format=csv,noheader}
${color2}│ Utilisation:${color} ${execi 3 /usr/bin/nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader,nounits} %
${color1}│${color} ${execibar 3 /usr/bin/nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader,nounits}

${color2}│ VRAM utilisée:${color} ${execi 3 /usr/bin/nvidia-smi --query-gpu=memory.used --format=csv,noheader} MB
${color2}│ VRAM totale:${color} ${execi 86400 /usr/bin/nvidia-smi --query-gpu=memory.total --format=csv,noheader} MB
${color1}│${color} ${execibar 3 /usr/bin/nvidia-smi --query-gpu=memory.used --format=csv,noheader,nounits}

${color2}│ Température:${color} ${execi 3 /usr/bin/nvidia-smi --query-gpu=temperature.gpu --format=csv,noheader} °C

${color2}│ Fréquence:${color} ${execi 3 /usr/bin/nvidia-smi --query-gpu=clocks.current.graphics --format=csv,noheader} MHz

${color1}└──────────────────────────────${color}
]]
